{
    "name": "sequential-posts-block",
    "version": "1.2.0",
    "description": "Gutenberg block: N posts sequentially following the current post.",
    "main": "build/index.js",
    "private": true,
    "scripts": {
        "build": "wp-scripts build",
        "start": "wp-scripts start",
        "lint:js": "wp-scripts lint-js src",
        "format": "wp-scripts format src"
    },
    "devDependencies": {
        "@wordpress/scripts": "^27.0.0"
    }
}
